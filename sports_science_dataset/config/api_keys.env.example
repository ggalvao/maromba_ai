# API Keys Configuration
# OpenAI API key for AI-powered filtering and quality assessment
OPENAI_API_KEY=your_openai_api_key_here

# Email for PubMed API (required by NCBI)
NCBI_EMAIL=your_email@example.com

# Optional: Semantic Scholar API key (increases rate limits)
SEMANTIC_SCHOLAR_API_KEY=optional_semantic_scholar_key

# Rate limiting configuration
PUBMED_RATE_LIMIT=3  # requests per second
SEMANTIC_SCHOLAR_RATE_LIMIT=100  # requests per minute
ARXIV_RATE_LIMIT=10  # requests per minute
OPENAI_RATE_LIMIT=60  # requests per minute